<h1>All Trips</h1>

<%= link_to 'Add a Trip', new_trip_path %>

<div class="ui divider"></div>

<ul>
  <% @trips.each do |trip| %>
    <li><%= link_to trip.name, trip_path(trip.id) %></li>
  <% end %>
</ul>
